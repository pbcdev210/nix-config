{
  plugins.mini = {
    enable = true;
    modules = {
      pairs = {
        modes = { };
      };

      surround = { };
      ai = { };


      splitjoin = { };
      hipatterns = { };
      clue = { };
    };
  };
}
